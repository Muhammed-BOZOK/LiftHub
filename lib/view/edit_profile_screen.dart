
import 'package:flutter/material.dart';

class EditProfileScrren extends StatelessWidget {
  const EditProfileScrren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body:  const Center(
        child: Text('EDİT PROFILE'),
      ),
      
    );
  }
}